<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/place_item_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:layout_width="0dp"
        android:layout_height="75dp"
        android:layout_margin="8dp"
        android:layout_weight="2"
        android:id="@+id/place_image"
        tools:src="@drawable/cairo_cover"
        />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center"
        android:layout_weight="7">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cairo"
            android:id="@+id/name_place_text"
            android:textColor="?attr/colorPrimary"
            android:textSize="20sp"
            android:padding="4dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/menu_about_cairo"
            android:textColor="?attr/colorPrimary"
            android:id="@+id/address_place_text"
            android:padding="4dp"/>

    </LinearLayout>

    <ImageView
        android:layout_width="0dp"
        android:layout_height="24dp"
        app:srcCompat="@drawable/ic_menu_open"
        android:layout_gravity="center"
        android:layout_weight="1"/>

</LinearLayout>